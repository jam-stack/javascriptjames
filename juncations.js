/*let coffeeIsGrinding = false;
const pressGrindbeans = () => {
    if(coffeeIsGrinding) {
        console.log("stopping the grind");
    }else {
        console.log("Grinding is about to begin");
        coffeeIsGrinding = true;
    }
}
pressGrindbeans();
pressGrindbeans();*/

/*const drinkorder = (size, typeofdrink) => {
    console.log(`pour ${size} type of drink ${typeofdrink}`);
}
drinkorder ("tall","coffee");
drinkorder ("tea");
drinkorder ("bovril")*/

/*const addup = (num1, num2) => {
    return num1 + num2;
}
addup (7,3);
console.log(addup(2,5));*/

/*const multiplyByNineFifths = (celsius) => {
    return celsius * (9/5);
};
const getFahrenheit = (celsius) => {
    return multiplyByNineFifths (celsius) +32;
};
console.log("the temperature is " + getFahrenheit(15) + "•F");
//output: The tmperature is 59•F*/

/*let orderCount = 0;

const takeOrder = (topping1, topping2) => {
    console.log(`pizza with ${topping}`);
    orderCount++;
    console.log(`order number ${orderCount}`)
}
takeOrder("pineapple","chicken");
takeOrder("pepperoni", "mushroom");*/

/*et cashmachine=0

const takeMoney = (pinNum, account, withDraw)
console.log("cashwithdraw");failure*/

/*let accBal = 400;
let accPin = 1234;

const cashMachine = (pinNum, cash) => {

if (pinNum == accPin && cash <= accBal) {

console.log(`Your PIN is correct and the ${cash} you requested is being dispensed!`)    

    } else if (pinNum != accPin) {

        console.log(`PIN number is incorrect!. Please try again.`);

    } else {

        console.log(`Insufficient funds in your account! Please check and try again.`);

    }

    const sandwichOrder = (top1, top2, top3, top4, top5)

}

cashMachine(1234, 300);

cashMachine(1235, 300);

cashMachine(1234, 410);

cashMachine(1235, 500);*/






